name: CI
on: [push]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Set up MySQL
        run: sudo /etc/init.d/mysql start
      - name: Test MySQL
        run: echo "SELECT 2+3" |mysql -uroot
